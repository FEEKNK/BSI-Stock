# 📋 แผนการพัฒนาและปรับปรุงระบบ BSI-Stock (Development Roadmap)

> **บันทึกไว้สำหรับดำเนินงานต่อในวันพรุ่งนี้**  
> แบ่งการทำงานออกเป็น **4 รอบ (4 Batches)** ตามลำดับความสำคัญ ความเสี่ยง และ Dependency ของไฟล์ เพื่อให้ทดสอบได้ง่ายและไม่เกิดปัญหาตอนรวมโค้ด

---

## 🛡️ รอบที่ 1: Data Integrity & Safety (ทำก่อน - ความเสี่ยงต่ำ / สำคัญสูงสุด)
> **เป้าหมาย:** ป้องกันข้อมูลสต็อกผิดพลาดหรือติดลบจากการเบิกจ่าย

### 1.1 ป้องกันสต็อกติดลบ (Negative Stock Prevention)
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/components/Dispensing/DispensingForm.jsx` (หน้าเบิก/จ่ายสินค้า)
  - `src/pages/BarcodePage.jsx` (หน้าสแกนบาร์โค้ดเบิก/รับ)
  - `server/index.js` หรือ API Endpoint สำหรับทำรายการเบิก
- **รายละเอียดการทำงาน:**
  - เพิ่มการตรวจสอบ (Validation) ก่อนบันทึกการเบิก: ถ้า `จำนวนที่ต้องการเบิก > สต็อกคงเหลือของไซส์นั้น` ให้แสดงข้อความเตือนและไม่อนุญาตให้กดยืนยัน
  - แสดงตัวเลขสต็อกคงเหลือปัจจุบันข้างๆ ช่องกรอกจำนวน เพื่อให้ผู้ใช้ทราบทันที
  - เพิ่มการตรวจสอบที่ระดับ Backend API เพื่อความปลอดภัยของข้อมูล
- **การทดสอบ:**
  - ทดสอบเบิกสินค้าเกินจำนวนจริง -> ระบบต้องปฏิเสธและขึ้นแจ้งเตือน

---

## 📦 รอบที่ 2: ต่อยอดงานไซส์และประสบการณ์ใช้งาน (UI/UX Continuity) `[เสร็จสมบูรณ์ ✅]`
> **เป้าหมาย:** เพิ่มความสะดวกในการดูและค้นหาสต็อกรายไซส์ในหน้าจัดการสินค้า และปรับปรุงระบบกระดิ่งแจ้งเตือน

### 2.1 กรองสินค้าตามสถานะสต็อก (Filter by Stock Status) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/pages/ProductsPage.jsx`
  - `src/hooks/useProducts.js`
- **รายละเอียดการทำงาน:**
  - เพิ่ม Dropdown กรองสถานะ: `สถานะสต็อก: ทั้งหมด`, `🔴 มีไซส์หมดสต็อก`, `🟡 มีไซส์ใกล้หมด`, `🟢 สต็อกปกติทั้งหมด`
  - คัดกรองและประเมินสต็อกระดับรายไซส์แบบเรียลไทม์

### 2.2 แถวขยายดูสต็อกแยกไซส์ (Expand Row ▼ ดูสต็อกแยกไซส์) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/components/Products/ProductList.jsx`
- **รายละเอียดการทำงาน:**
  - ปุ่ม `▼ ดูไซส์` / `▲ ซ่อนไซส์` และปุ่ม master `ขยาย/ยุบทั้งหมด` ที่หัวตาราง
  - แถบขยายแสดงการ์ดไซส์แยกแต่ละไซส์ พร้อมจำนวนคงเหลือ ป้ายสถานะ และรหัสบาร์โค้ด
  - ปุ่ม 1-Click Copy รหัสบาร์โค้ดพร้อม Toast แจ้งเตือน

### 2.3 จัดกลุ่มรายการแจ้งเตือนในกระดิ่ง (Notification Dropdown Grouping) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/components/Layout/Header.jsx`
  - `src/components/Layout/Header.css`
  - `src/hooks/useAlerts.js`
- **รายละเอียดการทำงาน:**
  - จัดกลุ่มการแจ้งเตือนตามสินค้า พร้อมแท็บตัวกรองด่วน (`ทั้งหมด`, `หมดสต็อก`, `ใกล้หมด`)
  - ปุ่มสลับมุมมอง: 📦 จัดกลุ่มตามสินค้า (Grouped) / 📋 เรียงตามความด่วน 1:1 (List)
  - ปิดเมนูอัตโนมัติเมื่อคลิกนอกพื้นที่หรือกด Esc

---

## ✨ รอบที่ 3: ฟีเจอร์ใหม่และวิเคราะห์ข้อมูล (New Standalone Features) `[เสร็จสมบูรณ์ ✅]`
> **เป้าหมาย:** เพิ่มความสามารถใหม่ที่ไม่กระทบกับระบบเดิม

### 3.1 นำเข้าข้อมูลสินค้าจาก Excel (.xlsx Import) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/pages/ProductsPage.jsx`
  - `src/components/Products/ImportProductsModal.jsx`
  - `src/utils/excel.js`
  - `server/index.js` (`POST /api/products/bulk`)
- **รายละเอียดการทำงาน:**
  - ปุ่ม `Import Excel` และดาวน์โหลด Template มาตรฐาน (`product_import_template.xlsx`)
  - หน้าต่างพรีวิวข้อมูลและตรวจสอบความถูกต้องก่อนกดบันทึก
  - รองรับโหมด Upsert (อัปเดตข้อมูลเดิมและเพิ่มรายการใหม่)

### 3.2 กราฟแนวโน้มการเบิก/รับสินค้า (Stock Movement & Trend Charts) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/pages/DashboardPage.jsx`
  - `src/components/Dashboard/StockMovementChart.jsx`
  - `src/components/Dashboard/TopDispensedWidget.jsx`
  - `server/index.js` (`GET /api/dashboard/movement`)
- **รายละเอียดการทำงาน:**
  - กราฟแท่งเปรียบเทียบยอดเบิกออก (สีแดง) vs รับเข้า (สีเขียว) เลือกดู 7 วัน, 14 วัน, 30 วัน
  - วิดเจ็ตอันดับสินค้าที่มีการเบิกใช้สูงสุด (Top 5 Dispensed Products) พร้อมแถบเปอร์เซ็นต์และจำนวนครั้ง

---

## ⚙️ รอบที่ 4: Infrastructure & Optimization (ความเสถียรและประสิทธิภาพ) `[เสร็จสมบูรณ์ ✅]`
> **เป้าหมาย:** ปรับปรุงความเร็วของระบบและการสำรองข้อมูล

### 4.1 ระบบสำรองและกู้คืนข้อมูล (Backup & Restore) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/pages/SettingsPage.jsx`
  - `server/index.js` (`GET /api/backup`, `POST /api/restore`)
- **รายละเอียดการทำงาน:**
  - ปุ่ม `ดาวน์โหลดไฟล์สำรอง (.json)` สำรองข้อมูลครบทุกตาราง (สินค้า, สต็อก, ประวัติ, รหัสอ้างอิง)
  - ระบบกู้คืนฐานข้อมูล (Restore Database) พร้อมหน้าต่างยืนยันและสรุปจำนวนข้อมูลก่อนเขียนทับ

### 4.2 การแบ่งโค้ดเพื่อเพิ่มความเร็ว (Code Splitting & Lazy Loading) `[เสร็จแล้ว ✅]`
- **ไฟล์ที่เกี่ยวข้อง:**
  - `src/App.jsx`
  - `vite.config.js`
- **รายละเอียดการทำงาน:**
  - ใช้ `React.lazy()` และ `<Suspense>` แยกโหลดหน้าเว็บแบบ Dynamic
  - ปรับแต่ง `manualChunks` แยกไลบรารีขนาดใหญ่ออกเป็นชิ้นย่อย (react, charts, xlsx, scanner, icons)
  - ลดขนาด JS Bundle เริ่มต้นจาก 1.44 MB เหลือเพียง 200 KB (เร็วขึ้นกว่า 7 เท่า!)

---

## 🏆 สรุปผลการพัฒนาครบทั้ง 4 รอบ (100% Completed)
1. **รอบที่ 1:** ป้องกันสต็อกติดลบ + ระบบ Toast + เครื่องหมาย * บังคับกรอก `[เสร็จสมบูรณ์ ✅]`
2. **รอบที่ 2:** กรองสถานะสต็อก + คลี่ดูสต็อกรายไซส์ + กระดิ่งแจ้งเตือน `[เสร็จสมบูรณ์ ✅]`
3. **รอบที่ 3:** นำเข้าสินค้า Excel (.xlsx) + กราฟแนวโน้มและสินค้าเบิกใช้สูงสุด `[เสร็จสมบูรณ์ ✅]`
4. **รอบที่ 4:** สำรอง/กู้คืนฐานข้อมูล (Backup & Restore) + Code Splitting โหลดเร็ว 7x `[เสร็จสมบูรณ์ ✅]`
